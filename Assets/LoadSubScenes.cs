using UnityEngine;
using System.Collections;
using UnityEngine.SceneManagement;
using Unity.Entities;
using Unity.Entities.Serialization;
using Unity.Scenes;
using UnityEditor;

public class LoadSubScenes : MonoBehaviour
{
    /*
    public SceneAsset[] _SceneAssets;
    public EntitySceneReference[] _esr;
    public World _world;
    private void Start()
    {
        _world = new World("NewWorld", WorldFlags.Game);
        _esr = new EntitySceneReference[_SceneAssets.Length];
        for (int i = 0; i < _SceneAssets.Length; i++)
        {
            _esr[i] = new EntitySceneReference(_SceneAssets[i]);
            //SceneSystem.LoadSceneAsync(_world, _esr[i]);
        }
    }
    */
}
